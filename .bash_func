# .bash_aliases に alias を追記して再読み込み
add_alias() {
  bash_aliases=~/.bash_aliases
  usage="usage: $FUNCNAME [ALIAS] [COMMAND]"

  if [ $# = 0 ]; then
    echo $usage
    return 0
  fi

  echo -e "alias $1='$2'\n" >> $bash_aliases
  source $bash_aliases
}
